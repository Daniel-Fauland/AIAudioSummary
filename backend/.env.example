# --- General Settings ---
LOGGING_LEVEL = "INFO"  # Can be either: DEBUG, INFO, WARNING, ERROR, CRITICAL
FASTAPI_WELCOME_MSG = Hello from the fastapi backend! Access the swagger docs at '/docs'.
ENVIRONMENT = development  # "development" or "production"
ALLOWED_ORIGINS = http://localhost:3000  # Comma-separated CORS origins

# --- Prompt Template Settings ---
PROMPT_TEMPLATE_DIRECTORY = "./prompt_templates"

# --- Database Settings ---
# Full async SQLAlchemy connection string.
# For local dev, run PostgreSQL locally or via Docker and set this accordingly.
DATABASE_URL = postgresql+asyncpg://aias:changeme@localhost:5432/aias_db

# Shared JWT validation secret — must match AUTH_SECRET in frontend/.env.local
AUTH_SECRET =

# Comma-separated list of admin emails to seed on startup
INITIAL_ADMINS =
